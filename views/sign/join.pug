extends ../layout

block content
    input#userid(type="text", placeholder="id")
    input#password(type="password" , placeholder="pw")
    input#name(type="text" , placeholder="name")
    button#send 보내기
        script(src="https://unpkg.com/axios/dist/axios.min.js")
    script(src="/js/join.js")
